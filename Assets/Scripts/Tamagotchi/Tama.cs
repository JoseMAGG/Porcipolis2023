using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tama : MonoBehaviour
{
    public System.TimeSpan diferencia;
    public System.DateTime tiempo;

    void Start()
    {
        /*
        tiempo = System.DateTime.Now;
        tiempo.AddMonths(3);
        diferencia = tiempo - System.DateTime.Now;
        print(diferencia);
        tiempo.AddMinutes(50);
        diferencia = tiempo - System.DateTime.Now;
        print(diferencia);
        print(diferencia.TotalDays);
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
